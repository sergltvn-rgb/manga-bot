"""Сервисные модули бота.

Эта папка — первый шаг распила монолитного `bot.py` на логические
подсистемы. Модули здесь не должны импортировать `bot.py` напрямую
(иначе циклический импорт). Если нужен `Bot`-инстанс или `Dispatcher` —
принимайте их параметром в factory-функцию или register_xxx(...).
"""
