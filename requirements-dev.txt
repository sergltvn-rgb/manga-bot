# Dev-инструменты: линтер, тест-раннер, pre-commit.
# Установка (один раз):
#   pip install -r requirements-dev.txt
#   pre-commit install
#
# Запуск вручную:
#   ruff check .
#   pytest tests/ -q
#   python scripts/check_no_shadowing.py
#   pre-commit run --all-files
ruff>=0.5.7,<0.7
pytest>=8.0
pre-commit>=3.7
